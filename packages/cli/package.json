{
  "name": "@hermes/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "fe-hermes": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@hermes/core": "workspace:*",
    "@inquirer/prompts": "^5.0.0",
    "commander": "^12.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "vitest": "^1.6.0"
  }
}
